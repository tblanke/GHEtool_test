[build-system]
requires = [
	"setuptools>=54",
	"wheel"
]
build-backend = "setuptools.build_meta"

[tool.mypy]
mypy_path ="GHEtool"
ignore_missing_imports = true

[tool.black]
line-length = 160